import { PageShell } from '@/components/dashboard/PageShell';

export default function MediaPage() {
  return <PageShell title="Media Library" subtitle="Manage images, videos, and all uploaded files" />;
}
