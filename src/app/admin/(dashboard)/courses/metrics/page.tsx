import { PageShell } from '@/components/dashboard/PageShell';

export default function CourseMetricsPage() {
  return <PageShell title="Course Metrics" subtitle="Track enrolments, completions, and progress" />;
}
